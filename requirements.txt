# Core web framework
flask==3.0.3
flask-cors==4.0.1

# Groq API (LLM + STT)
groq==0.8.0
httpx>=0.27.0

# Vector DB (Cloud connection — no local sqlite dependency needed)
qdrant-client==1.9.1

# Embeddings — unpin sub-dependencies to let HF builder resolve Python 3.10 compat
sentence-transformers==3.0.1
huggingface-hub==0.25.2

# PyTorch (CPU-only to keep Docker image smaller on HF Spaces)
torch==2.3.1
numpy==1.26.4

# HTTP client (used in stt.py)
requests>=2.31.0

# Environment
python-dotenv==1.0.1
