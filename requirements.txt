# Gradio (entry point for HF Spaces Gradio SDK)
gradio>=4.44.0

# LLM + STT
groq==0.8.0
httpx>=0.27.0

# Vector DB — connects to Qdrant Cloud via QDRANT_URL + QDRANT_API secrets
qdrant-client==1.9.1

# Embeddings — pin huggingface-hub to avoid HfFolder deprecation crash on HF Spaces
sentence-transformers==3.0.1
huggingface-hub==0.25.2

# PyTorch CPU — unpinned minor version for Python 3.10 compat on HF Spaces
torch>=2.3.0,<2.4.0
numpy>=1.24.0,<2.0.0

# HTTP client (used in stt.py SarvamSTT class)
requests>=2.31.0

# Environment
python-dotenv==1.0.1
